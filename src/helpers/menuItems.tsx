import { ItemIcon } from "./itemIcon"

export const menuItems = () => {
    const leftMenuItemsIcon = [
        {
            template : () => (
                <ItemIcon icon = 'pi-chart-bar' path='gerente'></ItemIcon>
            )
        }
    ]
}